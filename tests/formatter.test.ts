import { describe, it, expect } from 'vitest';\nimport {\n  formatarMoeda,\n  formatarNumero,\n  formatarData,\n  formatarPercentual,\n  truncarString,\n  capitalizarPrimeira,\n  capitalizarPalavras,\n  formatarBooleano,\n} from '../src/utils/formatter.js';\n\ndescribe('Formatter Utils', () => {\n  describe('formatarMoeda', () => {\n    it('deve formatar número como moeda brasileira', () => {\n      const resultado = formatarMoeda(1000);\n      expect(resultado).toContain('1');\n      expect(resultado).toContain('000');\n    });\n\n    it('deve formatar com 2 casas decimais', () => {\n      const resultado = formatarMoeda(1234.56);\n      expect(resultado).toContain('1');\n    });\n  });\n\n  describe('formatarNumero', () => {\n    it('deve formatar número com separador de milhares', () => {\n      const resultado = formatarNumero(1000);\n      expect(resultado).toContain('1');\n    });\n\n    it('deve formatar com casas decimais especificadas', () => {\n      const resultado = formatarNumero(1234.567, 2);\n      expect(resultado).toBeTruthy();\n    });\n  });\n\n  describe('formatarData', () => {\n    it('deve formatar data em formato brasileiro', () => {\n      const data = new Date('2025-01-15');\n      const resultado = formatarData(data);\n      expect(resultado).toContain('15');\n      expect(resultado).toContain('01');\n      expect(resultado).toContain('2025');\n    });\n\n    it('deve aceitar string de data', () => {\n      const resultado = formatarData('2025-01-15');\n      expect(resultado).toBeTruthy();\n    });\n  });\n\n  describe('formatarPercentual', () => {\n    it('deve formatar como percentual', () => {\n      const resultado = formatarPercentual(50);\n      expect(resultado).toContain('50');\n      expect(resultado).toContain('%');\n    });\n\n    it('deve respeitar casas decimais', () => {\n      const resultado = formatarPercentual(50.5, 1);\n      expect(resultado).toBeTruthy();\n    });\n  });\n\n  describe('truncarString', () => {\n    it('deve truncar string longa', () => {\n      const resultado = truncarString('Esta é uma string muito longa', 10);\n      expect(resultado.length).toBeLessThanOrEqual(10);\n      expect(resultado).toContain('...');\n    });\n\n    it('não deve truncar string curta', () => {\n      const resultado = truncarString('Curta', 10);\n      expect(resultado).toBe('Curta');\n    });\n  });\n\n  describe('capitalizarPrimeira', () => {\n    it('deve capitalizar primeira letra', () => {\n      const resultado = capitalizarPrimeira('joão');\n      expect(resultado).toBe('João');\n    });\n\n    it('deve converter resto para minúscula', () => {\n      const resultado = capitalizarPrimeira('JOÃO');\n      expect(resultado).toBe('João');\n    });\n  });\n\n  describe('capitalizarPalavras', () => {\n    it('deve capitalizar todas as palavras', () => {\n      const resultado = capitalizarPalavras('joão silva santos');\n      expect(resultado).toBe('João Silva Santos');\n    });\n  });\n\n  describe('formatarBooleano', () => {\n    it('deve converter true para \"Sim\"', () => {\n      expect(formatarBooleano(true)).toBe('Sim');\n    });\n\n    it('deve converter false para \"Não\"', () => {\n      expect(formatarBooleano(false)).toBe('Não');\n    });\n  });\n});\n
